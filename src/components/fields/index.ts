/**
 * 多种类型的输入组件
 * 包括：
 * 1. 普通输入组件
 * 2. 下拉选择 - 单级
 * 3. 下拉选择 - 多级
 * 4. 数字区间
 * 5. 价格区间
 * 6. 时间区间
 */
import Input from './InputField';
import SelectTreeField from './SelectTreeField';
import MoneyField from './MoneyField';

export default {
    Input,
    SelectTreeField,
    MoneyField
}
